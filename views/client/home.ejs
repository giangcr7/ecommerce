<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        body {
            background-color: #f8f9fa;
        }

        .hero-banner {
            background: linear-gradient(135deg, #0d6efd 0%, #0dcaf0 100%);
            color: white;
            padding: 60px 0;
            margin-bottom: 40px;
            border-radius: 0 0 20px 20px;
        }

        .product-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: none;
            border-radius: 12px;
            overflow: hidden;
            background: white;
            height: 100%;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }

        .product-img-wrapper {
            height: 220px;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            background-color: #fff;
            border-bottom: 1px solid #f0f0f0;
        }

        .product-img {
            max-height: 100%;
            max-width: 100%;
            object-fit: contain;
        }

        .price-text {
            color: #dc3545;
            /* Màu đỏ */
            font-size: 1.25rem;
            font-weight: 700;
        }

        .card-title a {
            text-decoration: none;
            color: #333;
            font-weight: 600;
        }

        .card-title a:hover {
            color: #0d6efd;
        }
    </style>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-bold text-uppercase" href="/">
                <i class="fas fa-laptop-code me-2"></i>Aliconcon Tech
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link active" href="/">Trang chủ</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Laptop</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Điện thoại</a></li>
                </ul>

                <div class="d-flex align-items-center gap-3">
                    <a href="/cart" class="btn btn-outline-light position-relative border-0">
                        <i class="fas fa-shopping-cart fa-lg"></i>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            0
                        </span>
                    </a>

                    <% if(user) { %>
                        <div class="dropdown">
                            <button class="btn btn-light dropdown-toggle" type="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user-circle me-1"></i>
                                <%= user.fullName || user.username %>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <% if(user.role && user.role.name==='ADMIN' ) { %>
                                    <li><a class="dropdown-item" href="/admin"><i class="fas fa-cog me-2"></i>Trang quản
                                            trị</a></li>
                                    <li>
                                        <hr class="dropdown-divider">
                                    </li>
                                    <% } %>
                                        <li><a class="dropdown-item" href="/profile"><i class="fas fa-user me-2"></i>Hồ
                                                sơ</a></li>
                                        <li><a class="dropdown-item text-danger" href="/logout"><i
                                                    class="fas fa-sign-out-alt me-2"></i>Đăng xuất</a></li>
                            </ul>
                        </div>
                        <% } else { %>
                            <div class="d-flex gap-2">
                                <a href="/login" class="btn btn-outline-light">Đăng nhập</a>
                                <a href="/register" class="btn btn-warning text-dark fw-bold">Đăng ký</a>
                            </div>
                            <% } %>
                </div>
            </div>
        </div>
    </nav>

    <div class="hero-banner text-center">
        <div class="container">
            <h1 class="display-4 fw-bold">Công Nghệ Đỉnh Cao - Giá Tốt Nhất</h1>
            <p class="lead mb-4">Săn sale Laptop Gaming, iPhone 15 Pro Max, Phụ kiện chính hãng ngay hôm nay.</p>
            <a href="#products-area" class="btn btn-light btn-lg fw-bold text-primary px-5 rounded-pill shadow">Mua
                Ngay</a>
        </div>
    </div>

    <div class="container mb-5" id="products-area">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3 class="fw-bold border-start border-4 border-primary ps-3">Sản phẩm nổi bật</h3>
            <a href="#" class="text-decoration-none">Xem tất cả <i class="fas fa-arrow-right"></i></a>
        </div>

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
            <% if(products && products.length> 0) { %>
                <% products.forEach(p=> { %>
                    <div class="col">
                        <div class="card product-card h-100">
                            <a href="/product/<%= p.id %>" class="product-img-wrapper">
                                <% let imgUrl='https://placehold.co/300x300?text=No+Image' ; if(p.image) {
                                    imgUrl=p.image.startsWith('http') ? p.image : '/' +
                                    p.image.replace(/^public[\\/]/, '' ).replace(/\\/g, '/' ); } %>
                                    <img src="<%= imgUrl %>" class="product-img" alt="<%= p.name %>">
                            </a>

                            <div class="card-body d-flex flex-column">
                                <div class="mb-2">
                                    <span class="badge bg-info text-dark bg-opacity-25 border border-info">
                                        <%= p.category ? p.category.name : 'Công nghệ' %>
                                    </span>
                                </div>

                                <h5 class="card-title text-truncate mb-1">
                                    <a href="/product/<%= p.id %>" title="<%= p.name %>">
                                        <%= p.name %>
                                    </a>
                                </h5>

                                <p class="card-text small text-muted mb-3" style="min-height: 40px;">
                                    <%= p.shortDesc ? p.shortDesc.substring(0, 50) + '...' : 'Mô tả đang cập nhật' %>
                                </p>

                                <div class="mt-auto">
                                    <div class="d-flex justify-content-between align-items-center mb-3">
                                        <span class="price-text">
                                            <%= p.price.toLocaleString('vi-VN') %> ₫
                                        </span>
                                        <% if(p.quantity> 0) { %>
                                            <small class="text-success"><i class="fas fa-check-circle"></i> Còn
                                                hàng</small>
                                            <% } else { %>
                                                <small class="text-danger"><i class="fas fa-times-circle"></i> Hết
                                                    hàng</small>
                                                <% } %>
                                    </div>

                                    <a href="/product/<%= p.id %>" class="btn btn-primary w-100 rounded-pill">
                                        <i class="fas fa-eye me-1"></i> Xem chi tiết
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <div class="col-12 text-center py-5">
                                <p class="text-muted fs-5">Chưa có sản phẩm nào được bày bán.</p>
                            </div>
                            <% } %>
        </div>
    </div>

    <footer class="bg-dark text-white pt-5 pb-3 mt-auto">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4">
                    <h5 class="text-uppercase fw-bold text-primary">Aliconcon Tech</h5>
                    <p class="small text-white-50">Chuyên cung cấp Laptop, Điện thoại chính hãng với giá tốt nhất thị
                        trường. Bảo hành uy tín.</p>
                </div>
                <div class="col-md-4 mb-4">
                    <h5 class="text-uppercase fw-bold">Liên hệ</h5>
                    <ul class="list-unstyled small text-white-50">
                        <li><i class="fas fa-map-marker-alt me-2"></i> TP. Hồ Chí Minh, Việt Nam</li>
                        <li><i class="fas fa-envelope me-2"></i> contact@aliconcon.com</li>
                        <li><i class="fas fa-phone me-2"></i> 0909.000.111</li>
                    </ul>
                </div>
            </div>
            <hr class="border-secondary">
            <div class="text-center small text-white-50">
                &copy; 2025 Aliconcon Shop. All rights reserved.
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>